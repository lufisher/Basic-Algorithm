//插入排序是最简单的排序方法，时间复杂度是O(n^2)
//要进行大量的赋值和移动操作
void InsertionSort(int array[],int first,int last){
    int i,j;
    int temp;//暂时的存储变量
	  for(i=first+1;i<last;i++){
		    temp=array[i];
		    j=i-1;
  	    while((j>=0)&&(array[j]>temp)){
		        array[j+1]=array[j];
		        j--;
	      }
	      if(j!=i-1)
	          array[j+1]=temp;
	  }
}
